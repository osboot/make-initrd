set-option -g status-position top
set-option -g status-left  '[#S] '
set-option -g status-right ' (Ctrl-Space + s)'
#set-option -g status off

set-option -g prefix C-Space

unbind-key C-b
bind-key C-Space send-prefix

