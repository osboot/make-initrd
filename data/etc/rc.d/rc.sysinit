#!/bin/sh

trap '' PIPE

# Rerun ourselves through initlog
if [ -z "$IN_INITLOG" -a -x /sbin/initlog ]; then
	exec initlog -r "$0"
fi

LOGLEVEL=1

# Fix console loglevel
dmesg -n "$LOGLEVEL"

# Save kernel environ
showenv > /.initrd/kernenv
