#!/bin/sh

. /.initrd/initenv

[ "${RDMODE-}" != 'live' ] ||
	exit 0

. uevent-sh-functions
. rdshell-sh-functions

[ -z "${ROOTFS_FOUND-}" ] && console_inactive && have_mountpoints && have_rootfs ||
	exit 0

echo ROOTFS_FOUND=1 >> /.initrd/initenv
echo 2 > /.initrd/telinit
