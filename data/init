#!/bin/sh

# Save kernel environment and init arguments
/bin/showenv > /.initrd/kernenv
echo "$*" > /.initrd/initargs

# Create storage for init environment
:> /.initrd/initenv

# Don't initialize SELinux
export SELINUX_INIT=1

exec /sbin/init-bin
