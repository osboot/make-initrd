#!/bin/sh -eu

omit=' 1 '
if [ -f "${1-}" ]; then
	while read i; do
		omit="$omit$i "
	done < "$1"
	shift
fi
cd /proc
for i in *; do
	[ -z "${omit##* $i *}" -o -z "${i##*[!0-9]*}" -o ! -f $i/exe ] ||
		kill ${1-} $i
done
